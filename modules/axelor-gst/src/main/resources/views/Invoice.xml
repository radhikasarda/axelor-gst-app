<?xml version="1.0" encoding="UTF-8"?>
<object-views xmlns="http://axelor.com/xml/ns/object-views" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://axelor.com/xml/ns/object-views
  http://axelor.com/xml/ns/object-views/object-views_5.0.xsd">

	<grid name="invoice-grid" title="Invoice" model="com.axelor.axelor.gst.db.Invoice">
		<field name="status" />
		<field name="company" />
		<field name="reference" />
		<field name="invoiceDate" />
		<field name="priority" />
		<field name="party" />
		<field name="partyContact" />
		<field name="invoiceAddress" />
		<field name="shippingAddress" />
		<field name="useInvoiceAddress" />
		<field name="netAmount" />
		<field name="netIGST" />
		<field name="netCGST" />
		<field name="netSGST" />
		<field name="grossAmount" />
		<field name="invoiceItemsList" />

	</grid>
	<form name="invoice-form" title="Invoice" model="com.axelor.axelor.gst.db.Invoice" width="large" onNew="action-invoice-method-set-defaults" onLoad="action-change-status-of-invoice" onSave="action-invoice-method-on-save-generate-reference">
		<panel title="Invoice" css="panel-blue">
			<panel colSpan="12">
				<field name="status" widget="NavSelect" selection="invoice-status-selection" />

			</panel>

			<field name="company" />
			<field name="reference" />
			<field name="invoiceDate" />
			<field name="priority" />
			<field name="party" onChange="action-invoice-method-onChange-party" />
			<field name="partyContact" />
			<field name="invoiceAddress" />
			<field name="shippingAddress" />
			<field name="useInvoiceAddress" onChange="action-invoice-method-onChange-party" />
		</panel>

		<panel-related field="invoiceItemsList" onChange="action-invoice-net-calculation"></panel-related>
		<panel>
			<field name="netAmount" />
			<field name="netIGST" />
			<field name="netCGST" />
			<field name="netSGST" />
			<field name="grossAmount" />
		</panel>
		<panel sidebar="true">
			<button name="btnValidate" title="Validate" onClick="action-change-status-of-invoice" />
			<button name="btnCancel" title="Cancel" onClick="action-button-cancel-on-click" />
		</panel>
	</form>

	<kanban name="invoice-kanban" title="All Invoice" model="com.axelor.axelor.gst.db.Invoice" columnBy="status" sequenceBy="priority" limit="10">
		<field name="company" />
		<field name="netAmount" />
		<field name="grossAmount" />
		<template><![CDATA[
 		<h4>{{company.name}}</h4>
		<div class="card-body">Gross Amount:{{grossAmount}}</div>
		<div class="card-body">Net Amount:{{netAmount}}</div>
    ]]></template>
	</kanban>



	<selection name="invoice-status-selection">
		<option value="draft">Draft</option>
		<option value="validated">Validated</option>
		<option value="paid">Paid</option>
		<option value="cancelled">Cancelled</option>
	</selection>

	<action-record name="action-invoice-method-set-defaults" model="com.axelor.axelor.gst.db.Invoice">
		<field name="company" expr="eval:__repo__(Company).all().fetchOne()" />
		<field name="invoiceDate" expr="eval: __datetime__ " />
		<field name="useInvoiceAddress" expr="true" />
	</action-record>

	<action-method name="action-invoice-net-calculation">
		<call class="com.axelor.gst.web.Controller" method="calculateInvoice" />
	</action-method>

	<action-attrs name="action-change-status-of-invoice">
		<attribute name="title" for="btnValidate" expr="Paid" />
		<attribute name="value" for="status" expr="validated" if="status=='draft'" />
		<attribute name="value" for="status" expr="paid" if="status=='validated'" />
		<attribute name="hidden" for="btnValidate" expr="true" if="status=='paid'" />
		
	</action-attrs>

	<action-attrs name="action-button-cancel-on-click">
		<attribute name="value" for="status" expr="cancelled" />
	</action-attrs>

	<action-method name="action-invoice-method-onChange-party">
		<call class="com.axelor.gst.web.Controller" method="fetchInvoiceData" />
	</action-method>
	
	<action-method name="action-invoice-method-on-save-generate-reference">
	<call class="com.axelor.gst.web.Controller" method="getReferenceInvoice" />
	</action-method>

</object-views>